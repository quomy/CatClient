{
  "name": "catclient-tags-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply catclient --local",
    "db:migrate:remote": "wrangler d1 migrations apply catclient --remote"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.73.0"
  }
}